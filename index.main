<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>NEON // OVERRIDE</title>

<style>
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700;800;900&display=swap');

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --cyan: #00f7ff;
    --pink: #ff00d9;
    --purple: #7a00ff;
    --blue: #005eff;
    --bg: #03030a;
}

html {
    scroll-behavior: smooth;
}

body {
    background: var(--bg);
    color: white;
    font-family: Orbitron, sans-serif;
    overflow-x: hidden;
    cursor: none;
}

/* ================= CURSOR ================= */

.cursor {
    position: fixed;
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background: white;
    box-shadow:
        0 0 10px white,
        0 0 30px var(--cyan),
        0 0 60px var(--pink);
    pointer-events: none;
    z-index: 99999;
    transform: translate(-50%, -50%);
}

.cursor-ring {
    position: fixed;
    width: 45px;
    height: 45px;
    border: 1px solid var(--cyan);
    border-radius: 50%;
    pointer-events: none;
    z-index: 99998;
    transform: translate(-50%, -50%);
    transition: width .2s, height .2s;
}

/* ================= LOADER ================= */

.loader {
    position: fixed;
    inset: 0;
    background: #020207;
    z-index: 9999;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    transition: 1s;
}

.loader.hide {
    opacity: 0;
    pointer-events: none;
}

.loader-text {
    font-size: 25px;
    letter-spacing: 8px;
    color: var(--cyan);
    text-shadow: 0 0 20px var(--cyan);
    animation: flicker .12s infinite alternate;
}

.loader-bar {
    width: 300px;
    height: 3px;
    background: #111;
    margin-top: 30px;
    overflow: hidden;
}

.loader-progress {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, var(--cyan), var(--pink));
    box-shadow: 0 0 20px var(--cyan);
    animation: load 2.5s forwards;
}

@keyframes load {
    to { width: 100%; }
}

@keyframes flicker {
    from { opacity: .7; }
    to { opacity: 1; }
}

/* ================= CANVAS ================= */

#particles {
    position: fixed;
    inset: 0;
    z-index: -3;
}

/* ================= BACKGROUND ================= */

.grid {
    position: fixed;
    inset: -50%;
    z-index: -2;
    background-image:
        linear-gradient(rgba(0,247,255,.08) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,247,255,.08) 1px, transparent 1px);
    background-size: 70px 70px;
    transform: perspective(500px) rotateX(65deg);
    animation: gridMove 5s linear infinite;
    mask-image: linear-gradient(to bottom, transparent, black, transparent);
}

@keyframes gridMove {
    to {
        background-position: 0 70px;
    }
}

.noise {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 1000;
    opacity: .04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
}

.orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(100px);
    opacity: .35;
    z-index: -4;
}

.orb.one {
    width: 400px;
    height: 400px;
    background: var(--purple);
    top: 10%;
    left: -100px;
    animation: float1 8s ease-in-out infinite;
}

.orb.two {
    width: 350px;
    height: 350px;
    background: var(--pink);
    right: -100px;
    top: 40%;
    animation: float2 10s ease-in-out infinite;
}

.orb.three {
    width: 300px;
    height: 300px;
    background: var(--cyan);
    left: 40%;
    bottom: -100px;
    animation: float3 7s ease-in-out infinite;
}

@keyframes float1 {
    50% { transform: translate(150px, 100px) scale(1.3); }
}

@keyframes float2 {
    50% { transform: translate(-150px, -100px) scale(.8); }
}

@keyframes float3 {
    50% { transform: translate(100px, -150px) scale(1.2); }
}

/* ================= NAV ================= */

nav {
    position: fixed;
    top: 20px;
    left: 50%;
    transform: translateX(-50%);
    width: min(900px, 90%);
    padding: 15px 25px;
    border: 1px solid rgba(0,247,255,.25);
    background: rgba(5,5,15,.55);
    backdrop-filter: blur(20px);
    border-radius: 100px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 500;
    box-shadow: 0 0 40px rgba(0,247,255,.08);
}

.logo {
    color: var(--cyan);
    text-shadow: 0 0 15px var(--cyan);
    font-weight: 900;
    letter-spacing: 4px;
}

nav a {
    color: #aaa;
    text-decoration: none;
    margin-left: 25px;
    font-size: 11px;
    transition: .3s;
}

nav a:hover {
    color: white;
    text-shadow: 0 0 15px var(--cyan);
}

/* ================= HERO ================= */

.hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    position: relative;
    padding: 120px 20px;
}

.hero-content {
    transform-style: preserve-3d;
    transition: transform .15s ease-out;
}

.badge {
    display: inline-block;
    padding: 10px 18px;
    border: 1px solid var(--cyan);
    color: var(--cyan);
    font-size: 10px;
    letter-spacing: 4px;
    margin-bottom: 30px;
    box-shadow: 0 0 20px rgba(0,247,255,.2);
    animation: badgePulse 2s infinite;
}

@keyframes badgePulse {
    50% { box-shadow: 0 0 40px rgba(0,247,255,.6); }
}

.hero h1 {
    font-size: clamp(60px, 13vw, 180px);
    line-height: .8;
    font-weight: 900;
    letter-spacing: -8px;
    position: relative;
}

.glitch {
    position: relative;
    color: white;
    text-shadow:
        4px 0 var(--cyan),
        -4px 0 var(--pink);
    animation: glitch .8s infinite;
}

.glitch::before,
.glitch::after {
    content: attr(data-text);
    position: absolute;
    inset: 0;
    background: transparent;
}

.glitch::before {
    color: var(--cyan);
    transform: translate(3px);
    clip-path: inset(0 0 65% 0);
    animation: glitchTop 1.3s infinite linear alternate-reverse;
}

.glitch::after {
    color: var(--pink);
    transform: translate(-3px);
    clip-path: inset(65% 0 0 0);
    animation: glitchBottom .9s infinite linear alternate-reverse;
}

@keyframes glitchTop {
    0% { clip-path: inset(0 0 80% 0); }
    50% { clip-path: inset(20% 0 40% 0); }
    100% { clip-path: inset(5% 0 60% 0); }
}

@keyframes glitchBottom {
    0% { clip-path: inset(70% 0 0 0); }
    50% { clip-path: inset(40% 0 10% 0); }
    100% { clip-path: inset(80% 0 0 0); }
}

@keyframes glitch {
    0%, 100% { transform: skew(0); }
    20% { transform: skew(2deg); }
    21% { transform: skew(-4deg); }
    22% { transform: skew(0); }
}

.subtitle {
    margin-top: 40px;
    color: #888;
    font-size: 12px;
    letter-spacing: 5px;
}

.cta {
    margin-top: 50px;
    padding: 18px 35px;
    background: transparent;
    color: white;
    border: 1px solid var(--cyan);
    font-family: inherit;
    font-weight: bold;
    letter-spacing: 3px;
    cursor: none;
    position: relative;
    overflow: hidden;
    transition: .3s;
}

.cta::before {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--cyan);
    transform: translateX(-100%);
    transition: .4s;
    z-index: -1;
}

.cta:hover {
    color: #000;
    box-shadow:
        0 0 20px var(--cyan),
        0 0 60px var(--cyan);
}

.cta:hover::before {
    transform: translateX(0);
}

/* ================= SECTION ================= */

section {
    min-height: 100vh;
    padding: 140px 8%;
    position: relative;
}

.section-title {
    font-size: clamp(35px, 7vw, 90px);
    margin-bottom: 80px;
}

.section-title span {
    color: var(--cyan);
    text-shadow: 0 0 30px var(--cyan);
}

/* ================= CARDS ================= */

.cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 25px;
}

.card {
    min-height: 350px;
    border: 1px solid rgba(255,255,255,.1);
    background: rgba(255,255,255,.025);
    backdrop-filter: blur(15px);
    padding: 35px;
    position: relative;
    overflow: hidden;
    transition: transform .2s, border .3s;
    transform-style: preserve-3d;
}

.card::before {
    content: "";
    position: absolute;
    width: 200px;
    height: 200px;
    border-radius: 50%;
    background: var(--cyan);
    filter: blur(80px);
    opacity: 0;
    transition: .5s;
}

.card:hover {
    border-color: var(--cyan);
    box-shadow: 0 0 50px rgba(0,247,255,.15);
}

.card:hover::before {
    opacity: .2;
}

.card-number {
    font-size: 60px;
    color: rgba(255,255,255,.08);
    font-weight: 900;
}

.card h3 {
    margin-top: 70px;
    font-size: 25px;
    color: var(--cyan);
}

.card p {
    color: #777;
    font-size: 12px;
    line-height: 2;
    margin-top: 20px;
}

/* ================= BIG CENTER ================= */

.portal {
    width: min(600px, 80vw);
    height: min(600px, 80vw);
    margin: auto;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}

.ring {
    position: absolute;
    border-radius: 50%;
    border: 1px solid var(--cyan);
    box-shadow:
        0 0 20px var(--cyan),
        inset 0 0 20px var(--cyan);
}

.ring:nth-child(1) {
    width: 100%;
    height: 100%;
    animation: spin 15s linear infinite;
}

.ring:nth-child(2) {
    width: 75%;
    height: 75%;
    border-color: var(--pink);
    box-shadow:
        0 0 20px var(--pink),
        inset 0 0 20px var(--pink);
    animation: spinReverse 9s linear infinite;
}

.ring:nth-child(3) {
    width: 50%;
    height: 50%;
    border-color: #7a00ff;
    animation: spin 5s linear infinite;
}

.portal-core {
    width: 130px;
    height: 130px;
    border-radius: 50%;
    background: radial-gradient(circle, white, var(--cyan), var(--purple), transparent);
    box-shadow:
        0 0 40px var(--cyan),
        0 0 100px var(--purple),
        0 0 200px var(--pink);
    animation: corePulse 1.5s infinite alternate;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

@keyframes spinReverse {
    to { transform: rotate(-360deg); }
}

@keyframes corePulse {
    from { transform: scale(.8); filter: hue-rotate(0deg); }
    to { transform: scale(1.2); filter: hue-rotate(90deg); }
}

/* ================= TERMINAL ================= */

.terminal {
    max-width: 900px;
    margin: auto;
    border: 1px solid rgba(0,247,255,.3);
    background: #05050c;
    box-shadow: 0 0 80px rgba(0,247,255,.08);
}

.terminal-head {
    padding: 15px 20px;
    border-bottom: 1px solid #222;
    display: flex;
    gap: 8px;
}

.dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
}

.red { background: #ff3154; }
.yellow { background: #ffd000; }
.green { background: #00ff88; }

.terminal-body {
    padding: 35px;
    min-height: 300px;
    color: #00ff88;
    font-family: monospace;
    font-size: 14px;
    line-height: 2;
}

.typing::after {
    content: "▋";
    animation: blink .6s infinite;
}

@keyframes blink {
    50% { opacity: 0; }
}

/* ================= FOOTER ================= */

footer {
    padding: 80px 20px;
    text-align: center;
    border-top: 1px solid rgba(255,255,255,.08);
    color: #444;
    font-size: 10px;
    letter-spacing: 5px;
}

/* ================= REVEAL ================= */

.reveal {
    opacity: 0;
    transform: translateY(100px) scale(.9);
    transition: 1s cubic-bezier(.16,1,.3,1);
}

.reveal.show {
    opacity: 1;
    transform: translateY(0) scale(1);
}

/* ================= RESPONSIVE ================= */

@media(max-width: 800px) {
    nav {
        display: none;
    }

    .cards {
        grid-template-columns: 1fr;
    }

    .hero h1 {
        letter-spacing: -4px;
    }

    body {
        cursor: auto;
    }

    .cursor,
    .cursor-ring {
        display: none;
    }
}
</style>
</head>

<body>

<div class="loader">
    <div class="loader-text">INITIALIZING...</div>
    <div class="loader-bar">
        <div class="loader-progress"></div>
    </div>
</div>

<div class="cursor"></div>
<div class="cursor-ring"></div>

<canvas id="particles"></canvas>

<div class="grid"></div>
<div class="noise"></div>

<div class="orb one"></div>
<div class="orb two"></div>
<div class="orb three"></div>

<nav>
    <div class="logo">N//X</div>

    <div>
        <a href="#home">HOME</a>
        <a href="#system">SYSTEM</a>
        <a href="#portal">PORTAL</a>
    </div>
</nav>

<main>

<section class="hero" id="home">

    <div class="hero-content">

        <div class="badge">
            SYSTEM ONLINE // 2026
        </div>

        <h1>
            <span class="glitch" data-text="NEON">NEON</span>
            <br>
            <span style="color:#00f7ff;">VOID</span>
        </h1>

        <p class="subtitle">
            ENTER THE DIGITAL UNKNOWN
        </p>

        <button class="cta" onclick="activate()">
            SYSTEM OVERRIDE
        </button>

    </div>

</section>


<section id="system">

    <div class="section-title reveal">
        THE <span>SYSTEM</span>
    </div>

    <div class="cards">

        <div class="card reveal">
            <div class="card-number">01</div>
            <h3>QUANTUM</h3>
            <p>
                A visual interface built around chaos,
                motion, neon energy and impossible geometry.
            </p>
        </div>

        <div class="card reveal">
            <div class="card-number">02</div>
            <h3>NEURAL</h3>
            <p>
                Everything reacts to your movement.
                The interface watches. The interface responds.
            </p>
        </div>

        <div class="card reveal">
            <div class="card-number">03</div>
            <h3>UNKNOWN</h3>
            <p>
                There is no destination.
                Only deeper layers of the system.
            </p>
        </div>

    </div>

</section>


<section id="portal">

    <div class="section-title reveal">
        OPEN THE <span>PORTAL</span>
    </div>

    <div class="portal reveal">

        <div class="ring"></div>
        <div class="ring"></div>
        <div class="ring"></div>

        <div class="portal-core"></div>

    </div>

</section>


<section>

    <div class="section-title reveal">
        SYSTEM <span>LOG</span>
    </div>

    <div class="terminal reveal">

        <div class="terminal-head">
            <div class="dot red"></div>
            <div class="dot yellow"></div>
            <div class="dot green"></div>
        </div>

        <div class="terminal-body">
            <div id="terminalText" class="typing"></div>
        </div>

    </div>

</section>

</main>

<footer>
    N//X DIGITAL REALITY — NO LIMITS
</footer>


<script>

/* ================= LOADER ================= */

window.addEventListener("load", () => {
    setTimeout(() => {
        document.querySelector(".loader").classList.add("hide");
    }, 2700);
});


/* ================= CURSOR ================= */

const cursor = document.querySelector(".cursor");
const ring = document.querySelector(".cursor-ring");

let mouseX = 0;
let mouseY = 0;
let ringX = 0;
let ringY = 0;

document.addEventListener("mousemove", e => {

    mouseX = e.clientX;
    mouseY = e.clientY;

    cursor.style.left = mouseX + "px";
    cursor.style.top = mouseY + "px";
});

function animateCursor() {

    ringX += (mouseX - ringX) * .12;
    ringY += (mouseY - ringY) * .12;

    ring.style.left = ringX + "px";
    ring.style.top = ringY + "px";

    requestAnimationFrame(animateCursor);
}

animateCursor();


/* ================= PARTICLES ================= */

const canvas = document.getElementById("particles");
const ctx = canvas.getContext("2d");

let particles = [];

function resizeCanvas() {
    canvas.width = innerWidth;
    canvas.height = innerHeight;
}

resizeCanvas();
window.addEventListener("resize", resizeCanvas);

class Particle {

    constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * canvas.height;

        this.vx = (Math.random() - .5) * .5;
        this.vy = (Math.random() - .5) * .5;

        this.size = Math.random() * 2 + .5;

        this.color =
            Math.random() > .5
            ? "#00f7ff"
            : "#ff00d9";
    }

    update() {

        this.x += this.vx;
        this.y += this.vy;

        if (this.x < 0 || this.x > canvas.width)
            this.vx *= -1;

        if (this.y < 0 || this.y > canvas.height)
            this.vy *= -1;
    }

    draw() {

        ctx.beginPath();

        ctx.arc(
            this.x,
            this.y,
            this.size,
            0,
            Math.PI * 2
        );

        ctx.fillStyle
